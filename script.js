#typing{
    width: 80%;
    margin: 20px auto;
    color: white;
    font-size: 22px;
    line-height: 1.8;
    white-space: pre-line;
}

.page{
    text-align: center;
    animation: fadeIn 1s ease;
}

@keyframes fadeIn{
    from{
        opacity:0;
        transform:translateY(20px);
    }
    to{
        opacity:1;
        transform:translateY(0);
    }
}
